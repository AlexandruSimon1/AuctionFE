server {
    listen 80;
    server_name http://auction-simon-project.page;
    root /usr/share/nginx/html;
    index index.html;
    try_files $uri $uri/ /index.html =404;
}

server {
  listen 443;
  server_name https://auction-simon-project.page;
  root /usr/share/nginx/html;
  index index.html;
  ssl on;
  ssl_certificate auction-simon-project_page.crt;
  ssl_certificate_key auction-simon-project_page.key;
  location / {
    try_files $uri /index.html;  
  }
}